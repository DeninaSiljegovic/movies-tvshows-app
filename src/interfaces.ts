export interface Props {
    id: number;
    name: string;
    poster_path: string,    
    type: string
  }

export interface movieProps {
    id: number;
    title: string,
    poster_path: string,   
}

export interface basicData{
  itemId: string,
  type: string
}